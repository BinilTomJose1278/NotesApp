pipeline {
    agent any
    stages {
        stage('Check Docker') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'docker --version'
                    } else {
                        bat 'docker --version'
                    }
                }
            }
        }
    }
}
